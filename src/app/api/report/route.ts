import {NextResponse} from "next/server";
export async function POST(req:Request){try{const b=await req.json();if(!b.name||!b.companyName||!b.email||!b.privacyConsent)return NextResponse.json({error:"필수 정보와 개인정보 동의를 확인해 주세요."},{status:400});if(!/^\S+@\S+\.\S+$/.test(b.email))return NextResponse.json({error:"올바른 이메일 주소를 입력해 주세요."},{status:400});console.info("[TRIVIAN_REPORT_REQUEST]",JSON.stringify({receivedAt:new Date().toISOString(),admin:process.env.ADMIN_EMAIL||"MOCK_ADMIN",...b}));return NextResponse.json({ok:true,mock:!process.env.EMAIL_API_KEY,message:"신청이 접수되었습니다. TRIVIAN 검수 후 안내드리겠습니다."})}catch{return NextResponse.json({error:"요청을 처리하지 못했습니다."},{status:400})}}
